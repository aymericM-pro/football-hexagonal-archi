spring:
  application:
    name: footballApiSpring

server:
  servlet:
    context-path: /api/v1

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always

security:
  jwt:
    secret: c3f9a1d4b7e28fa94c3d2b6e7f1a9d0c4f7e3b2a1c8d9e0f3a2c1b7d4e9f6a3
    expiration-ms: 3600000

football:
  api:
    key: ${FOOTBALL_API_KEY}
    base-url: ${FOOTBALL_API_BASE_URL}
